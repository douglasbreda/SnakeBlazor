namespace SnakeBlazor.Utils
{
    public static class Enums
    {
        /// <summary>
        /// Defines the direction to where the snake should go
        /// </summary>
        public enum Direction
        {
            Up,
            Right,
            Left,
            Down
        }
    }
}
